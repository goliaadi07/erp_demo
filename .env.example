# Copy to .env for production. Never commit real secrets.
PORT=4000
JWT_SECRET=replace-with-a-long-random-secret

# SQLite file path (default: data/threadline.db). For Postgres later, swap the
# better-sqlite3 driver for pg / Prisma while keeping the same table shapes.
DATABASE_PATH=./data/threadline.db
